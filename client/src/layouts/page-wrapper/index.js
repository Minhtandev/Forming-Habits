import React from 'react'
import PropTypes from 'prop-types'

const Wrapper = props => {
  return (
    <>
        Wrapper
        { props.children }
    </>
  )
}

Wrapper.propTypes = {}

export default Wrapper